
{{ id }} is running.

  address    {{ user }}@{{ address }}:{{ ssh_port }}
  image      {{ image }} ({{ arch }})
  samples    {{ samples }}
  identity   {{ identity_file }}

Both agents keep their credentials on this host and reach the sandbox over SSH, so
nothing inside it ever holds a token.

  codex      /environment, then pick {{ id }}
  shell      cyber-sandbox ssh {{ id }}

Claude Code is registered as the SSH connection `{{ id }}`. The installed release does
not carry the `ssh` subcommand that opens one yet, so until it does, drive the sandbox
from codex or a shell.

Every packet the sandbox sends is audited or refused. Watch it with:

  cyber-sandbox audit tail {{ id }} --follow

Registered in {{ claude_settings }}
          and {{ codex_environments }}


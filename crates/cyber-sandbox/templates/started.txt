{{ id }} is running.

  image      {{ image }} ({{ arch }})
  samples    {{ samples }}
  work       {{ work_dir }}

Nothing inside it holds a credential. Both agents keep their tokens on this
host and run only their tool side in the machine, so a sample that reads every
file it can reach still finds none.

Open a shell in it:

  cyber-sandbox shell {{ id }}

Or hand it to codex: run `/environment` and pick {{ id }}.
Claude Code is registered against it too, but the release installed here cannot
open a registered machine of its own accord yet.

Every packet it sends is audited or refused. Watch them go by:

  cyber-sandbox audit tail {{ id }} --follow

Registered in {{ claude_settings }}
          and {{ codex_environments }}

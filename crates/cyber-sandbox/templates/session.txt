{% if created %}session {{ id }} is ready{% else %}session {{ id }} is back{% endif %}.

  image      {{ image }} ({{ arch }})
  samples    {{ samples }}
  work       {{ work_dir }}

Every packet it sends is audited or refused. Follow them from another terminal:

  cyber-sandbox audit {{ id }}

Come back to this session later with:

  cyber-sandbox shell --resume {{ id }}

